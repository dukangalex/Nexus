import test from "node:test"; import assert from "node:assert/strict"; import {ProxyCoreController} from "../src/core/controller.js"; import {compileChain} from "../src/core/chain-compiler.js"; test("controller binds detected kernel and adapter",()=>{const c=new ProxyCoreController();const d=c.load({inbounds:[],outbounds:[],route:{}});assert.equal(d.kernel,"sing-box");assert.equal(c.snapshot().adapter.chainMechanism,"detour");}); test("compiler preserves hop order",()=>{const x=compileChain("sing-box",{hops:[{id:"a"},{id:"b"}]});assert.deepEqual(x.hops.map(h=>h.id),["a","b"]);});