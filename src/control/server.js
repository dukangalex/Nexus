import http from "node:http"; import {securityPolicy} from "../core/security.js"; export function createServer({port=8787}={}){return http.createServer((req,res)=>{res.setHeader("content-type","application/json"); if(req.url==="/health")return res.end(JSON.stringify({ok:true,service:"nexus"}));if(req.url==="/security")return res.end(JSON.stringify(securityPolicy()));res.statusCode=404;res.end(JSON.stringify({ok:false,error:"not found"}));});}